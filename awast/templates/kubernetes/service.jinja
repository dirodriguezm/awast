apiVersion: v1
kind: Service
metadata:
  name: {{name}}
  namespace: {{namespace}}
  labels:
    service: {{name}}
spec:
  type: NodePort
  selector:
    app: {{name}}
  ports:
    - port: 80
      targetPort: 80